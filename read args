#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char* argv[])
{
  int num = atoi(argv[1]);
  cout << "num: " << num << endl;
  for(int i = 2; i < num+2; i++)
    cout << i << ": " << argv[i] << endl;

  cout << endl;
}
